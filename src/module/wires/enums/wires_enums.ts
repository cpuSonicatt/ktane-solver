import { Colour } from "../../../common/colour";

export enum WiresColour {
    RED = Colour.RED,
    YELLOW = Colour.YELLOW,
    BLUE = Colour.BLUE,
    BLACK = Colour.BLACK,
    WHITE = Colour.WHITE
}

export enum WireCut {
    FIRST = 0,
    SECOND = 1,
    THIRD = 2,
    FOURTH = 3,
    FIFTH = 4,
    SIXTH = 5
}